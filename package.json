{
  "name": "enterprise-knowledge-assistant",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "dev": "npx concurrently -n BACKEND,FRONTEND -c cyan,magenta \"cd backend && uvicorn app.api.main:app --reload --port 8000\" \"npm run dev --prefix frontend\"",
    "setup": "cd backend && python -m pip install --upgrade pip && pip install -r requirements.txt && cd ../frontend && npm install"
  },
  "devDependencies": {
    "concurrently": "8.2.2"
  }
}
